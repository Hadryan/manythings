import { SERVICE } from '@forrestjs/hooks'
export const SERVICE_NAME = `${SERVICE} redis`

export const REDIS_CONNECT = `${SERVICE_NAME}/connect`
export const REDIS_GEO_CONNECT = `${SERVICE_NAME}/geoConnect`
